# Public, browser-safe configuration (placeholders only — never commit real values)
VITE_SUPABASE_URL=https://your-project-ref.example.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_example_placeholder
VITE_SUPABASE_PROJECT_ID=your-project-ref

# Server-only mirrors of the public values
SUPABASE_URL=https://your-project-ref.example.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_example_placeholder
SUPABASE_PROJECT_ID=your-project-ref

# Server-only secrets. NEVER commit real values and never prefix them with VITE_.
# Configure these in the hosting/backend secret manager instead.
# SUPABASE_SERVICE_ROLE_KEY=set-in-secret-manager
# LOVABLE_CRON_SECRET=set-in-secret-manager
